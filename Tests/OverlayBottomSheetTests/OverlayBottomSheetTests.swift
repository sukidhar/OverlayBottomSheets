    import XCTest
    @testable import OverlayBottomSheet

    final class OverlayBottomSheetTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(OverlayBottomSheet().text, "Hello, World!")
        }
    }
